# Expense Tracker — Portfolio Case Study

## ข้อความสำหรับใส่การ์ดโปรเจกต์

**Expense Tracker | Full-stack Web Application**

เว็บบันทึกรายรับ–รายจ่ายภาษาไทยด้วย Flask, MySQL และ JavaScript รองรับบัญชีผู้ใช้แยกข้อมูล
ตัวกรองร่วมสำหรับตารางและกราฟ พร้อม Insight เปรียบเทียบรายจ่ายรายเดือน
นำขึ้นใช้งานจริงด้วย Render และ TiDB Cloud

- [Live demo](https://expense-tracker-sg.onrender.com/)
- [Source code](https://github.com/AoojunHappyman/Expense-Tracker)
- Developer: Pattanachai Sawetbunchoed

## ปัญหาและเป้าหมาย

การบันทึกยอดเพียงอย่างเดียวทำให้มองภาพรวมได้ยาก โปรเจกต์นี้รวมการบันทึก การค้นหารายการ
และการสรุปการใช้จ่ายไว้ในหน้าเดียว ผู้ใช้เลือกเดือน หมวดหมู่ หรือประเภทเพื่อดูข้อมูลที่เกี่ยวข้องได้
เป้าหมายของเวอร์ชันแรกคือเส้นทางใช้งานตั้งแต่สมัครบัญชีจนถึงวิเคราะห์ข้อมูลที่ทำงานครบวงจร

## ขอบเขตงานที่โปรเจกต์แสดง

- Frontend: ฟอร์ม ตาราง กราฟ ตัวกรอง และการแสดงผลบนหน้าจอขนาดต่าง ๆ
- Backend: REST API, validation, session, CSRF และการตรวจเจ้าของข้อมูล
- Database: ตารางบัญชี รายการ และตัวนับ rate limit พร้อม migration สำหรับข้อมูลเดิม
- Deployment: Waitress, environment configuration, HTTPS และการเชื่อมต่อฐานข้อมูลด้วย TLS
- Verification: Unit tests และ integration tests สำหรับบัญชี ข้อมูลแยกผู้ใช้ และ dashboard

ในการสัมภาษณ์ควรอธิบายส่วนที่เข้าใจและลงมือทำจริง รวมถึงบทบาทของเครื่องมือช่วยพัฒนา
ไม่อ้างจำนวนผู้ใช้ ผลธุรกิจ หรือประสิทธิภาพที่ยังไม่ได้วัด

## การตัดสินใจและปัญหาที่แก้

### ทำให้ข้อมูลทุกส่วนตรงกัน

เดิมตาราง ยอดรวม และ Insight โหลดแยกกัน ปัจจุบัน `/api/dashboard` ส่งข้อมูลเหล่านี้ในคำขอเดียว
และใช้ฟังก์ชันคำนวณร่วม ตัวกรองจึงมีความหมายเดียวกันในแต่ละส่วน
ฝั่ง JavaScript ตรวจลำดับคำขอเพื่อไม่ให้ผลจากตัวกรองเก่าแสดงทับผลใหม่

ข้อแลกเปลี่ยนคือเซิร์ฟเวอร์ยังอ่านรายการทั้งหมดของบัญชีก่อนกรอง หากข้อมูลเติบโตมาก
งานต่อไปคือวัด query time แล้วพิจารณา SQL aggregation และ pagination

### เปรียบเทียบเดือนอย่างถูกต้อง

การเลือกสองเดือนล่าสุดที่มีรายการอาจข้ามเดือนว่าง เช่น กันยายนเทียบกรกฎาคม
จึงเปลี่ยนเป็นเทียบเดือนตามปฏิทิน รวมการข้ามปีและนับเดือนว่างเป็นศูนย์
ถ้าเดือนก่อนเป็นศูนย์และเดือนนี้มียอด จะแจ้งว่าคำนวณเปอร์เซ็นต์ไม่ได้

Insight ระบุชื่อเดือนและยอดทั้งสองฝั่ง พร้อมบอกว่าเดือนปัจจุบันอาจยังไม่ครบเดือน
ไม่ได้อ้างว่าเป็นการเทียบช่วงจำนวนวันเท่ากัน

### แยกข้อมูลระหว่างบัญชี

API ใช้บัญชีจาก session เป็นเงื่อนไขเจ้าของรายการฝั่งเซิร์ฟเวอร์
ไม่ใช้ user ID ที่ผู้ใช้ส่งมาเป็นสิทธิ์เข้าถึงข้อมูล มี integration tests ตรวจการเข้าถึงข้ามบัญชี
คำขอที่แก้ข้อมูลต้องผ่าน CSRF และระบบบัญชีมีตัวนับ rate limit เก็บในฐานข้อมูล

### นำระบบไปใช้งานจริง

เว็บใช้ Render กับฐานข้อมูล TiDB และย้ายเว็บจาก Oregon มา Singapore ระหว่างพัฒนา
เพื่อให้บริการกับฐานข้อมูลอยู่ภูมิภาคใกล้กัน ไม่มีผล benchmark ก่อน–หลัง จึงไม่ระบุเปอร์เซ็นต์ที่เร็วขึ้น
production entrypoint ใช้ Waitress และตรวจค่า environment ที่จำเป็นก่อนเริ่มทำงาน

## หลักฐานใน repository

| เรื่อง | ไฟล์ |
|---|---|
| คำนวณ dashboard และเปรียบเทียบเดือน | [dashboard.py](dashboard.py) |
| API และการเข้าถึงรายการ | [app.py](app.py) |
| บัญชีและ session | [auth.py](auth.py) |
| ตัวกรองและการจัดการคำขอหน้าเว็บ | [static/js/script.js](static/js/script.js) |
| ตั้งค่า production | [production.py](production.py), [serve_render.py](serve_render.py) |
| ทดสอบเดือนว่าง ตัวกรอง และข้ามปี | [tests/test_dashboard.py](tests/test_dashboard.py) |
| ทดสอบข้อมูลแยกบัญชี | [tests/test_accounts_mysql.py](tests/test_accounts_mysql.py) |

ดูคำสั่งติดตั้งและทดสอบใน [README](README.md)

## ลำดับสาธิตประมาณ 2 นาที

1. เข้าสู่ระบบด้วยบัญชีสำหรับสาธิตและข้อมูลสมมติที่เตรียมไว้
2. แสดงภาพรวมแล้วเพิ่มรายจ่ายหนึ่งรายการ ให้เห็นยอดและกราฟเปลี่ยนตาม
3. เลือกเดือน หมวดหมู่ และประเภท อธิบายว่าทุกส่วนใช้ตัวกรองร่วมกัน
4. แสดง Insight ของเดือนที่มีและไม่มีข้อมูลเดือนก่อน
5. ทดลองแก้ไขและยกเลิก ก่อนแสดงหน้าจอขนาดมือถือและสลับโหมดสี
6. ปิดด้วยการอธิบายการแยกข้อมูลผู้ใช้ การทดสอบ และข้อจำกัดที่ยังเหลือ

## ภาพประกอบที่ควรเก็บก่อนนำเสนอ

ยังไม่ได้แนบภาพในเอกสารนี้ ให้ใช้ภาพจากแอปจริงและข้อมูลสมมติ:

- ภาพ desktop: การ์ดสรุป ตัวกรอง และกราฟที่มีข้อมูล
- ภาพผลการกรอง: ตารางกับ Insight ของเดือนที่เลือก
- ภาพ mobile: ฟอร์มเพิ่มรายการและเมนู

ไม่ควรมีรหัสผ่าน รหัสกู้บัญชี หรือข้อมูลการเงินจริงอยู่ในภาพ

## สถานะและงานต่อไป

เวอร์ชันแรกมีฟีเจอร์หลักและเว็บสาธิตพร้อมใช้งาน เหมาะสำหรับนำเสนอเป็นโปรเจกต์พอร์ต
สิ่งที่ยังต้องเตรียมสำหรับการนำเสนอคือภาพประกอบและข้อมูลสมมติในบัญชีสาธิตส่วนตัว
การสำรอง–กู้คืน TiDB จริงและการทดสอบโหลดเป็นงานที่ยังไม่ได้ยืนยันผล
ส่วนงบประมาณรายเดือน CSV และรายการประจำสามารถเป็นแผนเวอร์ชันถัดไป
